{
  "name": "bitnikgames-cms",
  "version": "0.1.0",
  "description": "Minimal git-backed CMS for Astro content-collection sites — password-gated admin UI that reads/writes markdown files straight to a GitHub repo via serverless functions. No database.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "npx vercel dev",
    "hash-password": "node scripts/hash-password.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  }
}
